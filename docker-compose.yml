version: "3.8"

services:
  postgres:
    image: postgres:17
    container_name: postgres-dev
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: english_platform_db
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: redis-dev
    restart: unless-stopped
    ports:
      - "6379:6379"

volumes:
  postgres-data:
